
import React from 'react'
import { storiesOf } from '@storybook/react'
import CBMAutoComplete from './AutoComplete'

storiesOf('Atoms.AutoComplete', module).add('Simple', () => (
    <CBMAutoComplete />
))
